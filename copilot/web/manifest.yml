# The manifest for the "web" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: web
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/'
  # You can specify a custom health check path. The default is "/".
  # healthcheck: '/'
  healthcheck:
    path: '/'
    port: 8080
    success_codes: '302'
    healthy_threshold: 3
    unhealthy_threshold: 2
    interval: 35s
    timeout: 30s
    grace_period: 90s

# Configuration for your containers and service.
image:
  location: public.ecr.aws/e9f6t9n0/digital-workspace:latest
  # Port exposed through your container to route traffic to it.
  port: 8080

cpu: 256       # Number of CPU units for the task.
memory: 1024 # Amount of memory in MiB used by the task.
count: 1       # Number of tasks that should be running in your service.
exec: true     # Enable running commands in your container.
network:
  connect: true # Enable Service Connect for intra-environment traffic between services.

# storage:
  # readonly_fs: true       # Limit to read-only access to mounted root filesystems.

# Optional fields for more advanced use-cases.
#
variables:                    # Pass environment variables as key value pairs.
  PORT: 8080
  DB_CONFIG_KEY: WEBDB_SECRET
  APP_ENV: dev
  DJANGO_DEBUG: False
  GUNICORN_ENABLE_ASYNC_PSYCOPG2: truew
  ELASTIC_APM_SERVER_TIMEOUT: 20s
  OPENSEARCH_URL: http://temporary-var


secrets:                      # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
  AUTHBROKER_CLIENT_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AUTHBROKER_CLIENT_ID
  AUTHBROKER_CLIENT_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AUTHBROKER_CLIENT_SECRET
  AUTHBROKER_URL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AUTHBROKER_URL
  AUTHOR_SUBSTITUTED: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AUTHOR_SUBSTITUTED
  AUTHOR_TO_BE_SUBSTITUTED: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AUTHOR_TO_BE_SUBSTITUTED
  AV_SIGNATURE_SECRET_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AV_SIGNATURE_SECRET_KEY
  AWS_ACCESS_KEY_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_ACCESS_KEY_ID
  AWS_REGION: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_REGION
  AWS_S3_CUSTOM_DOMAIN: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_S3_CUSTOM_DOMAIN
  AWS_SECRET_ACCESS_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_SECRET_ACCESS_KEY
  AWS_STORAGE_BUCKET_NAME: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_STORAGE_BUCKET_NAME
  CLAM_AV_DOMAIN: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/CLAM_AV_DOMAIN
  CLAM_AV_PASSWORD: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/CLAM_AV_PASSWORD
  CLAM_AV_USERNAME: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/CLAM_AV_USERNAME
  DJANGO_SECRET_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/DJANGO_SECRET_KEY
  ELASTIC_APM_SECRET_TOKEN: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/ELASTIC_APM_SECRET_TOKEN
  ELASTIC_APM_SERVER_URL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/ELASTIC_APM_SERVER_URL
  GOVUK_NOTIFY_API_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/GOVUK_NOTIFY_API_KEY
  HAWK_INCOMING_ACCESS_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/HAWK_INCOMING_ACCESS_KEY
  HAWK_INCOMING_SECRET_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/HAWK_INCOMING_SECRET_KEY
  NEW_ASSET_PATH: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/NEW_ASSET_PATH
  PAGE_PROBLEM_EMAIL_TEMPLATE_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PAGE_PROBLEM_EMAIL_TEMPLATE_ID
  PERM_SEC_NAME: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PERM_SEC_NAME
  PERSON_UPDATE_HAWK_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PERSON_UPDATE_HAWK_ID
  PERSON_UPDATE_HAWK_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PERSON_UPDATE_HAWK_KEY
  PERSON_UPDATE_WEBHOOK_URL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PERSON_UPDATE_WEBHOOK_URL
  PFM_AWS_ACCESS_KEY_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PFM_AWS_ACCESS_KEY_ID
  PFM_AWS_SECRET_ACCESS_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PFM_AWS_SECRET_ACCESS_KEY
  PFM_AWS_STORAGE_BUCKET_NAME: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PFM_AWS_STORAGE_BUCKET_NAME
  PFM_REGION: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PFM_REGION
  PROFILE_DELETED_EMAIL_TEMPLATE_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PROFILE_DELETED_EMAIL_TEMPLATE_ID
  PROFILE_DELETION_REQUEST_EMAIL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PROFILE_DELETION_REQUEST_EMAIL
  PROFILE_DELETION_REQUEST_EMAIL_TEMPLATE_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PROFILE_DELETION_REQUEST_EMAIL_TEMPLATE_ID
  PROFILE_EDITED_EMAIL_TEMPLATE_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/PROFILE_EDITED_EMAIL_TEMPLATE_ID
  SENTRY_DSN: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/SENTRY_DSN
  SUPPORT_REQUEST_EMAIL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/SUPPORT_REQUEST_EMAIL
  TWITTER_ACCESS_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TWITTER_ACCESS_SECRET
  TWITTER_ACCESS_TOKEN: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TWITTER_ACCESS_TOKEN
  TWITTER_DEPT_USER: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TWITTER_DEPT_USER
  TWITTER_OAUTH_CONSUMER_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TWITTER_OAUTH_CONSUMER_KEY
  TWITTER_OAUTH_CONSUMER_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TWITTER_OAUTH_CONSUMER_SECRET
  WAGTAIL_BASE_URL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/WAGTAIL_BASE_URL



# You can override any of the values defined above by environment.
environments:
  dev:
    http:
      alias: dev.workspace.uktrade.digital
